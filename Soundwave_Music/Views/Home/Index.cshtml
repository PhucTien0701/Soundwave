@using Soundwave_Music.Common.Helper
@model Soundwave_Music.Models.Song
@{
    ViewBag.Title = "Home Page";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
    // song new release
    var songvnnewrelease = (List<Soundwave_Music.Models.Song>)ViewBag.VNSongNewRelease;
    var songusuknewrelease = (List<Soundwave_Music.Models.Song>)ViewBag.USUKSongNewRelease;
    var songkoreanewrelease = (List<Soundwave_Music.Models.Song>)ViewBag.KoreaSongNewRelease;
    var songjapannewrelease = (List<Soundwave_Music.Models.Song>)ViewBag.JapanSongNewRelease;
    var songchinanewrelease = (List<Soundwave_Music.Models.Song>)ViewBag.ChinaSongNewRelease;
    // album new release
    var albumvnnewrelease = (List<Soundwave_Music.Models.Album>)ViewBag.VNAlbumNewRelease;
    var albumusuknewrelease = (List<Soundwave_Music.Models.Album>)ViewBag.USUKAlbumNewRelease;
    var albumkoreanewrelease = (List<Soundwave_Music.Models.Album>)ViewBag.KoreaAlbumNewRelease;
    var albumjapannewrelease = (List<Soundwave_Music.Models.Album>)ViewBag.JapanAlbumNewRelease;
    var albumchinanewrelease = (List<Soundwave_Music.Models.Album>)ViewBag.ChinaAlbumNewRelease;
    // video music new release
    var videovnnewrelease = (List<Soundwave_Music.Models.Video>)ViewBag.VNVideoNewRelease;
    var videousuknewrelease = (List<Soundwave_Music.Models.Video>)ViewBag.USUKVideoNewRelease;
    var videokoreanewrelease = (List<Soundwave_Music.Models.Video>)ViewBag.KoreaVideoNewRelease;
    var videojapannewrelease = (List<Soundwave_Music.Models.Video>)ViewBag.JapanVideoNewRelease;
    var videochinanewrelease = (List<Soundwave_Music.Models.Video>)ViewBag.ChinaVideoNewRelease;
}

<style>
    .carousel_img {
        height: 450px;
        width: 100% !important;
    }

    .album-poster {
        position: relative;
        display: block;
        border-radius: 7px;
        overflow: hidden;
        box-shadow: 0 15px 35px #3d2173a1;
        transition: all ease 0.4s;
    }

        .album-poster:hover {
            box-shadow: none;
            transform: scale(0.98) translateY(5px);
        }
    .album-poster + a h4 {
        padding-top: 10px;
    }
    .row_image {
        width: 100%;
        min-height: 50px;
    }
</style>

<div class="container" style="margin-top: 50px">
    <div id="HomepageCarouselIndicators" class="carousel slide" data-ride="carousel">
        <ol class="carousel-indicators">
            <li data-target="#HomepageCarouselIndicators" data-slide-to="0" class="active"></li>
            <li data-target="#HomepageCarouselIndicators" data-slide-to="1"></li>
            <li data-target="#HomepageCarouselIndicators" data-slide-to="2"></li>
        </ol>
        <div class="carousel-inner" style="height: 450px; width: 100% !important">
            <div class="carousel-item active">
                @if (User.Identity.IsAuthenticated == true)
                {
                    <a href="/UserSong/SongDetail/1056"><img class="carousel_img" src="~/Images/ImagesImagesCollection/thuongemchonao_banner.png" alt="First slide"></a>
                }
                else
                {
                    <a href="/GuestSong/SongDetail/1056"><img class="carousel_img" src="~/Images/ImagesImagesCollection/thuongemchonao_banner.png" alt="First slide"></a>
                }
            </div>
            <div class="carousel-item">
                <a href="#"><img class="carousel_img" src="~/Images/ImagesImagesCollection/bannernewsong.png" alt="Second slide"></a>
            </div>
            <div class="carousel-item">
                <img class="carousel_img" src="~/Images/_thumbs/ImagesGenre/ballad.png" alt="Third slide">
            </div>
        </div>
        <a class="carousel-control-prev" href="#HomepageCarouselIndicators" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="carousel-control-next" href="#HomepageCarouselIndicators" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
</div>
<br />
@if (User.Identity.IsAuthenticated == false)
{
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1 style="color: darkblue; font-size: 35px; text-align:center"><b>SONG NEW RELEASE</b></h1>
            </div>
            <br />
            <div class="col-md-12">
                <h1 style="color: #1adbd3; font-size: 30px"><b>VIETNAM</b></h1>
            </div>
            <br />
            <br />
            @foreach (var item in songvnnewrelease)
            {
                <div class="col-md-2">
                    <a href="@Url.Action("SongDetail", "GuestSong", new {id = item.Song_id})" class="album-poster">
                        <img class="row_image" src="@item.Image" alt="">
                    </a>
                    <a href="@Url.Action("SongDetail", "GuestSong", new {id = item.Song_id})">
                        <h4 style="font-size:20px; ">@item.Song_name</h4>
                    </a>
                    <a href="#" style="color:forestgreen">
                        <p style="font-size:15px">@item.Singer.Singer_name</p>
                    </a>
                </div>
            }
        </div>
    </div>
    <br />
}
else
{
    if (User.Identity.GetRole() == 2)
    {
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <h1 style="color: darkblue; font-size: 35px; text-align: center"><b>SONG NEW RELEASE</b></h1>
                </div>
                <br />
                <div class="col-md-12">
                    <h1 style="color: #1adbd3; font-size: 30px"><b>VIETNAM</b></h1>
                </div>
                <br />
                <br />
                @foreach (var item in songvnnewrelease)
                {
                    <div class="col-md-2">
                        <a href="@Url.Action("SongDetail", "UserSong", new {id = item.Song_id})" class="album-poster">
                            <img class="row_image" src="@item.Image" alt="">
                        </a>
                        <a href="@Url.Action("SongDetail", "UserSong", new {id = item.Song_id})">
                            <h4 style="font-size:20px; ">@item.Song_name</h4>
                        </a>
                        <a href="#" style="color:forestgreen">
                            <p style="font-size:15px">@item.Singer.Singer_name</p>
                        </a>
                    </div>
                }
            </div>
        </div>
    }
    else if (User.Identity.GetRole() == 1 || User.Identity.GetRole() == 3)
    {
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <h1 style="color: darkblue; font-size: 35px; text-align: center"><b>SONG NEW RELEASE</b></h1>
                </div>
                <br />
                <div class="col-md-12">
                    <h1 style="color: #1adbd3; font-size: 30px"><b>VIETNAM</b></h1>
                </div>
                <br />
                <br />
                @foreach (var item in songvnnewrelease)
                {
                    <div class="col-md-2">
                        <a href="@Url.Action("SongDetail", "UserSong", new {id = item.Song_id})" class="album-poster">
                            <img class="row_image" src="@item.Image" alt="">
                        </a>
                        <a href="@Url.Action("SongDetail", "UserSong", new {id = item.Song_id})">
                            <h4 style="font-size:20px; ">@item.Song_name</h4>
                        </a>
                        <a href="#" style="color:forestgreen">
                            <p style="font-size:15px">@item.Singer.Singer_name</p>
                        </a>
                    </div>
                }
            </div>
            <br />
            <div class="row">
                <div class="col-md-12">
                    <h1 style="color: #1adbd3; font-size: 30px"><b>US - UK</b></h1>
                </div>
                <br />
                <br />
                @foreach (var item in songusuknewrelease)
                {
                    <div class="col-md-2">
                        <a href="@Url.Action("SongDetail", "UserSong", new {id = item.Song_id})" class="album-poster">
                            <img class="row_image" src="@item.Image" alt="">
                        </a>
                        <a href="@Url.Action("SongDetail", "UserSong", new {id = item.Song_id})">
                            <h4 style="font-size:20px; ">@item.Song_name</h4>
                        </a>
                        <a href="#" style="color:forestgreen">
                            <p style="font-size:15px">@item.Singer.Singer_name</p>
                        </a>
                    </div>
                }
            </div>
            <br />
            <div class="row">
                <div class="col-md-12">
                    <h1 style="color: #1adbd3; font-size: 30px"><b>K-POP</b></h1>
                </div>
                <br />
                <br />
                @foreach (var item in songkoreanewrelease)
                {
                    <div class="col-md-2">
                        <a href="@Url.Action("SongDetail", "UserSong", new {id = item.Song_id})" class="album-poster">
                            <img class="row_image" src="@item.Image" alt="">
                        </a>
                        <a href="@Url.Action("SongDetail", "UserSong", new {id = item.Song_id})">
                            <h4 style="font-size:20px; ">@item.Song_name</h4>
                        </a>
                        <a href="#" style="color:forestgreen">
                            <p style="font-size:15px">@item.Singer.Singer_name</p>
                        </a>
                    </div>
                }
            </div>
            <br />
            <div class="row">
                <div class="col-md-12">
                    <h1 style="color: #1adbd3; font-size: 30px"><b>J-POP</b></h1>
                </div>
                <br />
                <br />
                @foreach (var item in songjapannewrelease)
                {
                    <div class="col-md-2">
                        <a href="@Url.Action("SongDetail", "UserSong", new {id = item.Song_id})" class="album-poster">
                            <img class="row_image" src="@item.Image" alt="">
                        </a>
                        <a href="@Url.Action("SongDetail", "UserSong", new {id = item.Song_id})">
                            <h4 style="font-size:20px; ">@item.Song_name</h4>
                        </a>
                        <a href="#" style="color:forestgreen">
                            <p style="font-size:15px">@item.Singer.Singer_name</p>
                        </a>
                    </div>
                }
            </div>
            <br />
            <div class="row">
                <div class="col-md-12">
                    <h1 style="color: #1adbd3; font-size: 30px"><b>C-POP</b></h1>
                </div>
                <br />
                <br />
                @foreach (var item in songchinanewrelease)
                {
                    <div class="col-md-2">
                        <a href="@Url.Action("SongDetail", "UserSong", new {id = item.Song_id})" class="album-poster">
                            <img class="row_image" src="@item.Image" alt="">
                        </a>
                        <a href="@Url.Action("SongDetail", "UserSong", new {id = item.Song_id})">
                            <h4 style="font-size:20px; ">@item.Song_name</h4>
                        </a>
                        <a href="#" style="color:forestgreen">
                            <p style="font-size:15px">@item.Singer.Singer_name</p>
                        </a>
                    </div>
                }
            </div>
        </div>
    }
}
<br />
<br />
<br />
<!--ALBUM-->
@if (User.Identity.IsAuthenticated == false)
{
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1 style="color: #fd1de8; font-size: 35px; text-align: center"><b>ALBUM NEW RELEASE</b></h1>
            </div>
            <br />
            <div class="col-md-12">
                <h1 style="color: #1adbd3; font-size: 30px"><b>VIETNAM</b></h1>
            </div>
            <br />
            <br />
            @foreach (var item in albumvnnewrelease)
            {
                <div class="col-md-2">
                    <a href="@Url.Action("AlbumDetail", "GuestAlbum", new {id = item.Album_id})" class="album-poster">
                        <img class="row_image" src="@item.Image" alt="">
                    </a>
                    <a href="@Url.Action("AlbumDetail", "GuestAlbum", new {id = item.Album_id})">
                        <h4 style="font-size:20px; ">@item.Album_name</h4>
                    </a>
                    <a href="#" style="color:forestgreen">
                        <p style="font-size:15px">@item.Singer.Singer_name</p>
                    </a>
                </div>
            }
        </div>
    </div>
    <br />
}
else
{
    if (User.Identity.GetRole() == 2)
    {
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <h1 style="color: #fd1de8; font-size: 35px; text-align: center"><b>ALBUM NEW RELEASE</b></h1>
                </div>
                <br />
                <div class="col-md-12">
                    <h1 style="color: #1adbd3; font-size: 30px"><b>VIETNAM</b></h1>
                </div>
                <br />
                <br />
                @foreach (var item in albumvnnewrelease)
                {
                    <div class="col-md-2">
                        <a href="@Url.Action("AlbumDetail", "UserAlbum", new {id = item.Album_id})" class="album-poster">
                            <img class="row_image" src="@item.Image" alt="">
                        </a>
                        <a href="@Url.Action("AlbumDetail", "UserAlbum", new {id = item.Album_id})">
                            <h4 style="font-size:20px; ">@item.Album_name</h4>
                        </a>
                        <a href="#" style="color:forestgreen">
                            <p style="font-size:15px">@item.Singer.Singer_name</p>
                        </a>
                    </div>
                }
            </div>
        </div>
    }
    else if (User.Identity.GetRole() == 1 || User.Identity.GetRole() == 3)
    {
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <h1 style="color: #fd1de8; font-size: 35px; text-align: center"><b>ALBUM NEW RELEASE</b></h1>
                </div>
                <br />
                <div class="col-md-12">
                    <h1 style="color: #1adbd3; font-size: 30px"><b>VIETNAM</b></h1>
                </div>
                <br />
                <br />
                @foreach (var item in albumvnnewrelease)
                {
                    <div class="col-md-2">
                        <a href="@Url.Action("AlbumDetail", "UserAlbum", new {id = item.Album_id})" class="album-poster">
                            <img class="row_image" src="@item.Image" alt="">
                        </a>
                        <a href="@Url.Action("AlbumDetail", "UserAlbum", new {id = item.Album_id})">
                            <h4 style="font-size:20px; ">@item.Album_name</h4>
                        </a>
                        <a href="#" style="color:forestgreen">
                            <p style="font-size:15px">@item.Singer.Singer_name</p>
                        </a>
                    </div>
                }
            </div>
            <br />
            <div class="row">
                <div class="col-md-12">
                    <h1 style="color: #1adbd3; font-size: 30px"><b>US - UK</b></h1>
                </div>
                <br />
                <br />
                @foreach (var item in albumusuknewrelease)
                {
                    <div class="col-md-2">
                        <a href="@Url.Action("AlbumDetail", "UserAlbum", new {id = item.Album_id})" class="album-poster">
                            <img class="row_image" src="@item.Image" alt="">
                        </a>
                        <a href="@Url.Action("AlbumDetail", "UserAlbum", new {id = item.Album_id})">
                            <h4 style="font-size:20px; ">@item.Album_name</h4>
                        </a>
                        <a href="#" style="color:forestgreen">
                            <p style="font-size:15px">@item.Singer.Singer_name</p>
                        </a>
                    </div>
                }
            </div>
            <br />
            <div class="row">
                <div class="col-md-12">
                    <h1 style="color: #1adbd3; font-size: 30px"><b>K-POP</b></h1>
                </div>
                <br />
                <br />
                @foreach (var item in albumkoreanewrelease)
                {
                    <div class="col-md-2">
                        <a href="@Url.Action("AlbumDetail", "UserAlbum", new {id = item.Album_id})" class="album-poster">
                            <img class="row_image" src="@item.Image" alt="">
                        </a>
                        <a href="@Url.Action("AlbumDetail", "UserAlbum", new {id = item.Album_id})">
                            <h4 style="font-size:20px; ">@item.Album_name</h4>
                        </a>
                        <a href="#" style="color:forestgreen">
                            <p style="font-size:15px">@item.Singer.Singer_name</p>
                        </a>
                    </div>
                }
            </div>
            <br />
            <div class="row">
                <div class="col-md-12">
                    <h1 style="color: #1adbd3; font-size: 30px"><b>J-POP</b></h1>
                </div>
                <br />
                <br />
                @foreach (var item in albumjapannewrelease)
                {
                    <div class="col-md-2">
                        <a href="@Url.Action("AlbumDetail", "UserAlbum", new {id = item.Album_id})" class="album-poster">
                            <img class="row_image" src="@item.Image" alt="">
                        </a>
                        <a href="@Url.Action("AlbumDetail", "UserAlbum", new {id = item.Album_id})">
                            <h4 style="font-size:20px; ">@item.Album_name</h4>
                        </a>
                        <a href="#" style="color:forestgreen">
                            <p style="font-size:15px">@item.Singer.Singer_name</p>
                        </a>
                    </div>
                }
            </div>
            <br />
            <div class="row">
                <div class="col-md-12">
                    <h1 style="color: #1adbd3; font-size: 30px"><b>C-POP</b></h1>
                </div>
                <br />
                <br />
                @foreach (var item in albumchinanewrelease)
                {
                    <div class="col-md-2">
                        <a href="@Url.Action("AlbumDetail", "UserAlbum", new {id = item.Album_id})" class="album-poster">
                            <img class="row_image" src="@item.Image" alt="">
                        </a>
                        <a href="@Url.Action("AlbumDetail", "UserAlbum", new {id = item.Album_id})">
                            <h4 style="font-size:20px; ">@item.Album_name</h4>
                        </a>
                        <a href="#" style="color:forestgreen">
                            <p style="font-size:15px">@item.Singer.Singer_name</p>
                        </a>
                    </div>
                }
            </div>
        </div>
    }
}
<br />
<br />
<br />
<!--VIDEO-->
@if (User.Identity.IsAuthenticated == false)
{
    <div class="container">
        <div class="row">
            <div class="col-md-12">
                <h1 style="color: #ff006e; font-size: 35px; text-align: center"><b>VIDEO NEW RELEASE</b></h1>
            </div>
            <br />
            <div class="col-md-12">
                <h1 style="color: #1adbd3; font-size: 30px"><b>VIETNAM</b></h1>
            </div>
            <br />
            <br />
            @foreach (var item in videovnnewrelease)
            {
                <div class="col-md-2">
                    <a href="@Url.Action("VideoDetail", "GuestVideo", new {id = item.Video_id})" class="album-poster">
                        <img class="row_image" src="@item.Image" alt="">
                    </a>
                    <a href="@Url.Action("VideoDetail", "GuestVideo", new {id = item.Video_id})">
                        <h4 style="font-size:20px; ">@item.Video_name</h4>
                    </a>
                    <a href="#" style="color:forestgreen">
                        <p style="font-size:15px">@item.Singer.Singer_name</p>
                    </a>
                </div>
            }
        </div>
    </div>
    <br />
}
else
{
    if (User.Identity.GetRole() == 2)
    {
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <h1 style="color: #ff006e; font-size: 35px; text-align: center"><b>VIDEO NEW RELEASE</b></h1>
                </div>
                <br />
                <div class="col-md-12">
                    <h1 style="color: #1adbd3; font-size: 30px"><b>VIETNAM</b></h1>
                </div>
                <br />
                <br />
                @foreach (var item in videovnnewrelease)
                {
                    <div class="col-md-2">
                        <a href="@Url.Action("VideoDetail", "UserVideo", new {id = item.Video_id})" class="album-poster">
                            <img class="row_image" src="@item.Image" alt="">
                        </a>
                        <a href="@Url.Action("VideoDetail", "UserVideo", new {id = item.Video_id})">
                            <h4 style="font-size:20px; ">@item.Video_name</h4>
                        </a>
                        <a href="#" style="color:forestgreen">
                            <p style="font-size:15px">@item.Singer.Singer_name</p>
                        </a>
                    </div>
                }
            </div>
        </div>
    }
    else if (User.Identity.GetRole() == 1 || User.Identity.GetRole() == 3)
    {
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <h1 style="color: #ff006e; font-size: 35px; text-align: center"><b>VIDEO NEW RELEASE</b></h1>
                </div>
                <br />
                <div class="col-md-12">
                    <h1 style="color: #1adbd3; font-size: 30px"><b>VIETNAM</b></h1>
                </div>
                <br />
                <br />
                @foreach (var item in videovnnewrelease)
                {
                    <div class="col-md-2">
                        <a href="@Url.Action("VideoDetail", "UserVideo", new {id = item.Video_id})" class="album-poster">
                            <img class="row_image" src="@item.Image" alt="">
                        </a>
                        <a href="@Url.Action("VideoDetail", "UserVideo", new {id = item.Video_id})">
                            <h4 style="font-size:20px; ">@item.Video_name</h4>
                        </a>
                        <a href="#" style="color:forestgreen">
                            <p style="font-size:15px">@item.Singer.Singer_name</p>
                        </a>
                    </div>
                }
            </div>
            <br />
            <div class="row">
                <div class="col-md-12">
                    <h1 style="color: #1adbd3; font-size: 30px"><b>US - UK</b></h1>
                </div>
                <br />
                <br />
                @foreach (var item in videousuknewrelease)
                {
                    <div class="col-md-2">
                        <a href="@Url.Action("VideoDetail", "UserVideo", new {id = item.Video_id})" class="album-poster">
                            <img class="row_image" src="@item.Image" alt="">
                        </a>
                        <a href="@Url.Action("VideoDetail", "UserVideo", new {id = item.Video_id})">
                            <h4 style="font-size:20px; ">@item.Video_name</h4>
                        </a>
                        <a href="#" style="color:forestgreen">
                            <p style="font-size:15px">@item.Singer.Singer_name</p>
                        </a>
                    </div>
                }
            </div>
            <br />
            <div class="row">
                <div class="col-md-12">
                    <h1 style="color: #1adbd3; font-size: 30px"><b>K-POP</b></h1>
                </div>
                <br />
                <br />
                @foreach (var item in videokoreanewrelease)
                {
                    <div class="col-md-2">
                        <a href="@Url.Action("VideoDetail", "UserVideo", new {id = item.Video_id})" class="album-poster">
                            <img class="row_image" src="@item.Image" alt="">
                        </a>
                        <a href="@Url.Action("VideoDetail", "UserVideo", new {id = item.Video_id})">
                            <h4 style="font-size:20px; ">@item.Video_name</h4>
                        </a>
                        <a href="#" style="color:forestgreen">
                            <p style="font-size:15px">@item.Singer.Singer_name</p>
                        </a>
                    </div>
                }
            </div>
            <br />
            <div class="row">
                <div class="col-md-12">
                    <h1 style="color: #1adbd3; font-size: 30px"><b>J-POP</b></h1>
                </div>
                <br />
                <br />
                @foreach (var item in videojapannewrelease)
                {
                    <div class="col-md-2">
                        <a href="@Url.Action("VideoDetail", "UserVideo", new {id = item.Video_id})" class="album-poster">
                            <img class="row_image" src="@item.Image" alt="">
                        </a>
                        <a href="@Url.Action("VideoDetail", "UserVideo", new {id = item.Video_id})">
                            <h4 style="font-size:20px; ">@item.Video_name</h4>
                        </a>
                        <a href="#" style="color:forestgreen">
                            <p style="font-size:15px">@item.Singer.Singer_name</p>
                        </a>
                    </div>
                }
            </div>
            <br />
            <div class="row">
                <div class="col-md-12">
                    <h1 style="color: #1adbd3; font-size: 30px"><b>C-POP</b></h1>
                </div>
                <br />
                <br />
                @foreach (var item in videochinanewrelease)
                {
                    <div class="col-md-2">
                        <a href="@Url.Action("VideoDetail", "UserVideo", new {id = item.Video_id})" class="album-poster">
                            <img class="row_image" src="@item.Image" alt="">
                        </a>
                        <a href="@Url.Action("VideoDetail", "UserVideo", new {id = item.Video_id})">
                            <h4 style="font-size:20px; ">@item.Video_name</h4>
                        </a>
                        <a href="#" style="color:forestgreen">
                            <p style="font-size:15px">@item.Singer.Singer_name</p>
                        </a>
                    </div>
                }
            </div>
        </div>
    }
}
